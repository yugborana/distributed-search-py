# Coordinator dependencies
fastapi==0.115.0
uvicorn==0.30.0
aiohttp>=3.9.0  # PERF-03: C-accelerated HTTP client for shard fan-out (replaces httpx)
orjson==3.10.0
numpy==1.26.4
etcd3==0.12.0
protobuf<=3.20.3
grpcio>=1.60.0
tantivy>=0.22.0
redis==5.0.0
pyyaml==6.0.2
fastembed==0.4.1
uvloop>=0.19.0    # PERF-02: 2-4x faster event loop (Linux only, used in Docker)
httptools>=0.6.0  # PERF-11: C-accelerated HTTP parser for uvicorn
